# 02 Delta Against Old SRS

Baseline yang digunakan adalah `docs/context/SRS_CORELASI_Final_New.md`. Jangan memakai `SRS_CORELASI_Final_New(updated).md` sebagai baseline final karena file tersebut terlalu banyak menggeser struktur lama.

| Section SRS Lama | Kode Requirement Lama | Isi Lama Ringkas | Kondisi Implementasi Aktual | Status | Rekomendasi Perubahan | Bukti Codebase |
| ---------------- | --------------------- | ---------------- | --------------------------- | ------ | --------------------- | -------------- |
| 3.2 FR-AKS | FR-AKS-01 | Login Admin/Guru/Siswa. | Login email/password ada dan ditambah CSRF + JWT refresh cookie. | UPDATE | Pertahankan requirement login, tambahkan kalimat ringkas bahwa sesi memakai token dan cookie aman; detail teknis masuk NFR. | `accounts/views.py`: `LoginView`, `CsrfTokenView`, `CustomTokenRefreshView`; `frontend/src/services/api.ts`. |
| 3.2 FR-AKS | FR-AKS-02 | Redirect dashboard sesuai peran. | Route role-based dan redirect setelah login ada. | KEEP | Pertahankan. | `frontend/src/pages/auth/LoginPage.tsx`; `frontend/src/utils/authRedirect.ts`; `frontend/src/app/router.tsx`. |
| 3.2 FR-AKS | FR-AKS-03 | Pengguna melihat profil sendiri. | Profil tersedia untuk semua role. | KEEP | Pertahankan. | `accounts/views.py`: `ProfileView`; routes `*/profile` di `frontend/src/app/router.tsx`. |
| 3.2 FR-AKS | FR-AKS-04 | Pengguna mengubah kata sandi mandiri. | Endpoint dan halaman ganti password tersedia. | KEEP | Pertahankan. | `accounts/views.py`: `ChangePasswordView`; `frontend/src/pages/auth/ChangePasswordPage.tsx`. |
| 3.2 FR-AKS | FR-AKS-05 | Akses berdasarkan peran utama dan penugasan. | Implementasi memakai role utama `admin/guru/siswa`, assignment guru, frontend guard, dan backend scope. | UPDATE | Jelaskan bahwa Guru Pengampu, Guru Piket, dan Wali Kelas adalah penugasan di bawah role Guru, bukan role utama terpisah. | `accounts/models.py`: `is_pengampu`, `is_piket_today`; `shared/access.py`; `AssignmentGuard.tsx`; `RoleGuard.tsx`. |
| 3.2 FR-AKS | Tambahan ke FR-AKS | Tidak ada quick login demo di SRS lama. | Showcase quick login ada untuk demo allowlist. | ADD | Masukkan sebagai catatan opsional/demo di modul akun jika dibutuhkan; jangan jadikan fitur inti akademik. | `accounts/views.py`: `ShowcaseLoginView`; `config/settings.py`: `SHOWCASE_MODE`, `SHOWCASE_ACCOUNT_EMAILS`. |
| 3.2 FR-AKS | Tambahan ke FR-AKS | Self-delete admin belum tertulis. | Admin/superuser tidak dapat menghapus akunnya sendiri. | ADD | Tambahkan aturan keamanan akses administratif. | `accounts/views.py`: `UserViewSet.destroy`. |
| 3.2 FR-ADM | FR-ADM-01 | Admin tambah/ubah/nonaktif/aktifkan akun. | CRUD user ada; field `status` ada; delete juga ada; self-delete diblokir. | UPDATE | Pertahankan tetapi tulis sesuai implementasi: admin mengelola akun dan status, serta dapat menghapus akun lain. | `accounts/models.py`: `User.status`; `accounts/views.py`: `UserViewSet`. |
| 3.2 FR-ADM | FR-ADM-02 | Admin reset kata sandi. | Ada alur permintaan reset dan action resolve menghasilkan temporary password. | UPDATE | Ubah wording menjadi "mengelola permintaan reset kata sandi dan menerbitkan password sementara". | `accounts/models.py`: `PasswordResetRequest`; `accounts/views.py`: `PasswordResetRequestViewSet.resolve`. |
| 3.2 FR-ADM | FR-ADM-03 | Admin mengelola Guru Piket dan Wali Kelas. | Guru piket di `JadwalPiket`; wali kelas di `Kelas.wali_kelas`; assignment juga muncul di user serializer. | UPDATE | Jelaskan sumber data aktual: jadwal piket dan wali kelas pada data kelas. | `schedules/models.py`: `JadwalPiket`; `academic/models.py`: `Kelas.wali_kelas`; `accounts/serializers.py`: assignment object. |
| 3.2 FR-ADM | FR-ADM-04 | Admin mengelola tahun ajaran, semester, kelas, mapel. | Model, serializer, viewset, dan endpoint ada. | KEEP | Pertahankan. | `academic/models.py`; `academic/views.py`; `academic/urls.py`. |
| 3.2 FR-ADM | FR-ADM-05 | Tepat satu semester aktif pada satu waktu. | Ada status semester dan validasi tanggal/overlap; bukti enforcement tepat satu aktif belum terlihat di serializer/model yang dibaca. | UPDATE | Tulis lebih konservatif: "admin dapat mengelola status semester aktif"; jika ingin klaim tepat satu aktif, perlu cek manual/enforcement tambahan. | `academic/models.py`: `Semester.status`; `academic/serializers.py`: `SemesterSerializer.validate`; `UNCERTAIN` untuk enforce exactly-one active. |
| 3.2 FR-ADM | FR-ADM-06 | Keanggotaan siswa pada kelas semester aktif. | User punya FK `kelas`; Kelas punya `tahun_ajaran`. Tidak terlihat model membership history per semester. | UPDATE | Ubah menjadi "admin mengelola kelas siswa aktif melalui data pengguna/kelas"; jangan klaim history membership. | `accounts/models.py`: `User.kelas`; `academic/models.py`: `Kelas.tahun_ajaran`. |
| 3.2 FR-JWL | FR-JWL-01 | Admin menyusun jadwal per kelas/mapel/guru/hari/jam/semester aktif. | Implemented. | KEEP | Pertahankan. | `schedules/models.py`: `JadwalPelajaran`; `schedules/views.py`: `JadwalPelajaranViewSet`. |
| 3.2 FR-JWL | FR-JWL-02 | Admin mengubah/membatalkan/menyesuaikan sesi jadwal. | Create/update/delete jadwal ada. | UPDATE | Gunakan istilah CRUD jadwal; "membatalkan" berarti delete/remove, bukan cancellation workflow khusus. | `schedules/views.py`: `create`, `update`, `destroy`. |
| 3.2 FR-JWL | FR-JWL-03 | Jadwal ditampilkan sesuai role dan keterkaitan kelas. | Implemented dengan filter siswa/guru. | KEEP | Pertahankan. | `schedules/views.py`: `get_queryset`; `frontend/src/app/router.tsx`: `/guru/schedules`, `/siswa/schedules`. |
| 3.2 FR-ABS | FR-ABS-01 | Guru Pengampu mencatat absensi manual per sesi. | Implemented melalui create dan batch; guru harus bisa mengajar kelas/mapel. | KEEP | Pertahankan. | `attendance/views.py`: `AbsensiSiswaViewSet.create`, `batch`; `shared/access.py`: `can_teach_subject`. |
| 3.2 FR-ABS | FR-ABS-02 | Guru Piket menangani sakit/izin/alpa/koreksi; Admin override. | Koreksi dan verify ada untuk guru piket/admin; override admin ada. | KEEP | Pertahankan, sebut endpoint koreksi dan override. | `attendance/views.py`: `PermintaanKoreksiViewSet.verify`, `AbsensiSiswaViewSet.override`; `shared/access.py`: `is_duty_teacher`. |
| 3.2 FR-ABS | FR-ABS-03 | Riwayat absensi per siswa/kelas/semester aktif. | Model menyimpan siswa, kelas, mapel, tanggal, status; semester tidak langsung pada `AbsensiSiswa`. | UPDATE | Jangan klaim field semester langsung; semester diturunkan dari kelas/jadwal/konteks akademik bila diperlukan. | `attendance/models.py`: `AbsensiSiswa`; `schedules/models.py`: `JadwalPelajaran.semester`. |
| 3.2 FR-ABS | FR-ABS-04 | Rekap/riwayat berdasarkan scope tanggung jawab. | Implemented di queryset dan reports. | KEEP | Pertahankan. | `attendance/views.py`: `get_queryset`; `reports/views.py`: `AttendanceReportView`; `shared/access.py`: `teacher_class_ids`. |
| 3.2 FR-ABS | FR-ABS-05 | Ekspor rekap absensi. | Ada halaman/laporan absensi; bukti export file aktual belum terlihat dari endpoint yang dibaca. | UPDATE | Jika tidak ada tombol export nyata, tulis "menampilkan laporan absensi"; klaim export perlu cek manual UI/service. | `reports/views.py`: `AttendanceReportView`; frontend `/admin/reports/attendance`; `UNCERTAIN` untuk export file. |
| 3.2 FR-PMB | FR-PMB-01 | Guru mengelola materi sederhana. | Implemented, materi bisa file/link via `file_url` dan status. | KEEP | Pertahankan. | `learning/models.py`: `Materi`; `learning/views.py`: `MateriViewSet`; `/api/learning/materi/`. |
| 3.2 FR-PMB | FR-PMB-02 | Guru membuat/memperbarui tugas dengan deadline, status, submission mode. | Tugas memiliki deadline, file_url, status; submission mode khusus tidak terlihat. | UPDATE | Pertahankan deadline/status, hapus/kurangi klaim "mode submission" jika tidak ada field eksplisit. | `learning/models.py`: `Tugas`; `learning/views.py`: `TugasViewSet`. |
| 3.2 FR-PMB | FR-PMB-03 | Siswa akses materi/tugas publik kelasnya. | Implemented dengan filter `status="Dipublikasikan"` dan `kelas=user.kelas`. | KEEP | Pertahankan. | `learning/views.py`: `MateriViewSet.get_queryset`, `TugasViewSet.get_queryset`. |
| 3.2 FR-PMB | FR-PMB-04 | Siswa submit/update satu submission aktif dan Late setelah deadline. | Implemented via `update_or_create`, status `Late` jika lewat due date; resubmit setelah dinilai ditolak. | KEEP | Pertahankan. | `learning/views.py`: `SubmissionViewSet.create`; `learning/models.py`: `Submission`. |
| 3.2 FR-PMB | FR-PMB-05 | Guru melihat status submission. | Implemented dengan filter `tugas__guru=user`. | KEEP | Pertahankan. | `learning/views.py`: `SubmissionViewSet.get_queryset`; frontend `/guru/assignments/:id`, `/guru/grading`. |
| 3.2 FR-PMB | FR-PMB-06 | Guru memberi nilai dan feedback. | Implemented dengan grade 0-100 dan feedback. | KEEP | Pertahankan. | `learning/views.py`: `SubmissionViewSet.grade`. |
| 3.2 FR-PMB | FR-PMB-07 | Nilai manual sederhana dan nilai akhir operasional. | UI nilai manual ada; backend model nilai manual khusus tidak terlihat. | UPDATE | Tulis sebagai fitur UI operasional/rekap sederhana; jangan klaim model nilai akhir kompleks. | `frontend/src/pages/guru/ManualGradingPage.tsx`; `learning/models.py` hanya `Submission.grade`. |
| 3.2 FR-PMB | FR-PMB-08 | Siswa melihat nilai/feedback. | Implemented di route siswa nilai dan submission/grade API. | KEEP | Pertahankan. | `frontend/src/pages/siswa/GradesPage.tsx`; `reports/views.py`: `GradeReportView`; `learning/models.py`: `Submission.grade`, `feedback`. |
| 3.2 FR-PMB | FR-PMB-09 | Ekspor rekap nilai. | Ada laporan nilai; export file perlu cek manual. | UPDATE | Tulis "menampilkan rekap nilai"; klaim export hanya jika UI export terbukti. | `reports/views.py`: `GradeReportView`; frontend `/admin/reports/grades`, `/guru/reports/classes`. |
| 3.2 FR-JRN | FR-JRN-01 | Guru mengisi jurnal pertemuan. | Implemented. | KEEP | Pertahankan. | `journals/models.py`: `JurnalPertemuan`; `journals/views.py`: `JurnalPertemuanViewSet`. |
| 3.2 FR-JRN | FR-JRN-02 | Admin dan wali kelas memantau jurnal. | Admin/guru scope ada; wali kelas melalui `teacher_class_ids`; frontend admin/guru journals. | UPDATE | Jelaskan scope guru termasuk kelas diampu/perwalian; jangan tulis wali kelas sebagai role utama. | `journals/views.py`: `get_queryset`; `shared/access.py`: `teacher_class_ids`; routes `/admin/journals`, `/guru/journals`. |
| 3.2 FR-JRN | FR-JRN-03 | Rekap operasional gabungkan absensi, nilai, jurnal. | Operational report ada untuk admin; detail agregasi perlu disesuaikan dengan implementasi. | UPDATE | Pertahankan sebagai rekap operasional, tetapi jangan klaim semua agregasi jika tidak terbukti di field output. | `reports/views.py`: `OperationalReportView`; frontend `/admin/reports/operational`. |
| 3.2 FR-JRN | FR-JRN-04 | Filter rekap berdasarkan kelas/mapel/user/periode dalam scope. | Report endpoints dan frontend filters ada sebagian; perlu cek detail filter manual jika ingin lengkap. | UPDATE | Tulis umum: laporan menyediakan filter sesuai data akademik/scope yang tersedia. | `reports/views.py`; frontend report pages under `frontend/src/pages/admin/*ReportsPage.tsx`, `frontend/src/pages/guru/*ReportsPage.tsx`. |
| 3.3 NFR | Security | HTTPS, password hash, role access, validasi input. | Security jauh lebih spesifik di implementasi: secure cookies, CSRF/CORS, HSTS/CSP, throttling, upload validation. | UPDATE | Masukkan ringkasan security penting; detail deploy/tunnel jangan masuk panjang. | `config/settings.py`, `config/runtime.py`, `config/middleware.py`, `learning/views.py`. |
| 3.3 NFR | Backup & Recovery | Data dapat dicadangkan dan dipulihkan. | Backup/restore scripts dan systemd timer ada. | UPDATE | Tulis ringkas di NFR/operasi, detail tetap di runbook. | `scripts/backup-production.sh`, `scripts/restore-production.sh`, `scripts/verify-backup.sh`, `deploy/systemd/corelasi-backup.timer`. |
| 2.3 Lingkungan Operasi | Lingkungan umum browser/server. | Production detail ada, tetapi terlalu teknis untuk SRS akademik. | DO NOT IMPORT | Masukkan hanya ringkas: aplikasi web di browser modern dengan backend API dan database relasional. Detail Cloudflare/Tailscale/WSL2/Docker tetap di runbook. | `docs/production-runbook.md`, `docker-compose.production.yml`. |
