# 06 Non Functional Evidence

| Kategori NFR | Kondisi Aktual | Relevan Masuk SRS? | Rekomendasi Penulisan SRS | Bukti Codebase/Config |
| ------------ | -------------- | ------------------ | ------------------------- | --------------------- |
| Security | HTTPS/security headers dikonfigurasi di runtime dan middleware; CSP, permissions policy, HSTS, nosniff, referrer policy, X-Frame-Options. | Ya, ringkas | Tulis bahwa sistem menerapkan proteksi keamanan web standar untuk akses production; detail header spesifik cukup di runbook. | `corelasi-backend/config/settings.py`: security settings; `config/middleware.py`: `SecurityHeadersMiddleware`; `docs/production-runbook.md`: HSTS/CSP. |
| Authentication/session | JWT access token 15 menit; refresh token 7 hari, rotate dan blacklist; refresh token disimpan cookie; access token di memory frontend. | Ya | Tulis ringkas: sesi memakai token pendek dan refresh cookie aman; jangan jelaskan algoritma terlalu teknis. | `config/settings.py`: `SIMPLE_JWT`, `AUTH_REFRESH_COOKIE_*`; `accounts/cookies.py`; `frontend/src/services/sessionStore.ts`; `frontend/src/services/api.ts`. |
| Authorization/RBAC | Role utama admin/guru/siswa; guard frontend; permission dan queryset scope backend; assignment guru pengampu/piket/wali kelas. | Ya | Jelaskan role utama dan assignment guru sebagai scope akses. | `frontend/src/components/shared/RoleGuard.tsx`; `AssignmentGuard.tsx`; `shared/access.py`; app `views.py`. |
| Data validation | Serializer dan view melakukan validasi role, kelas, tanggal semester, siswa-kelas, password, grade 0-100, payload list batch. | Ya | Tulis bahwa input divalidasi di boundary API dan form; contoh tidak perlu terlalu banyak. | `academic/serializers.py`: `SemesterSerializer.validate`; `attendance/views.py`; `learning/views.py`; `accounts/views.py`: `ChangePasswordView`. |
| File upload limitation | Upload dibatasi ukuran, tipe ekstensi/MIME, signature file, nama file aman, dan throttle upload. | Ya | Masukkan sebagai NFR security/data integrity. | `learning/views.py`: `FileUploadView`, `ALLOWED_UPLOAD_TYPES`, `has_allowed_file_signature`; `config/settings.py`: `MAX_UPLOAD_SIZE`, `THROTTLE_UPLOAD_RATE`. |
| Performance | Frontend memakai lazy pages; performance test ringan sebelumnya pass 24 request dengan 0 error. | Ya, ringkas | Tulis target respons wajar untuk demo/sekolah; angka benchmark boleh ditempatkan di laporan pengujian, bukan SRS utama. | `frontend/src/app/lazyPages.ts`; `Laporan_Pengujian_CORELASI/Evidence/Performance/performance testing.md` bila digunakan editor. |
| Reliability/recovery | Health endpoint, Docker healthcheck, restart policy, deployment script, smoke test. | Sebagian | Masukkan health/readiness dan recovery umum; detail Docker/systemd tetap runbook. | `config/views.py`; `config/urls.py`; `docker-compose.production.yml`; `scripts/smoke-production.sh`; `scripts/deploy-linux.sh`. |
| Backup/restore | Script backup verified, restore dengan safety backup, verify backup, systemd timer. | Ya, ringkas | Tulis data dapat dicadangkan dan dipulihkan; detail command tidak masuk SRS. | `scripts/backup-production.sh`; `scripts/restore-production.sh`; `scripts/verify-backup.sh`; `deploy/systemd/corelasi-backup.timer`; `docs/production-runbook.md`. |
| Compatibility/browser | Aplikasi web React ditargetkan browser modern desktop; SRS lama menyebut Chrome/Edge/Firefox. | Ya | Pertahankan browser modern di Windows/Linux/macOS; hindari klaim mobile native. | `frontend/package.json`; `frontend/src/app/router.tsx`; SRS lama section 3.3. |
| Maintainability | Backend modular per app domain; frontend route role-based dan lazy page; docs/runbook/handoff ada. | Ya | Tulis struktur modular dan dokumentasi operasional sebagai maintainability. | `corelasi-backend/apps/*`; `frontend/src/app/router.tsx`; `frontend/src/app/lazyPages.ts`; `docs/production-runbook.md`. |
| Logging/monitoring | Logging JSON/file config tersedia; health endpoint tersedia. Monitoring eksternal belum terlihat. | Ya sebagian | Tulis logging server-side dan health endpoint; jangan klaim observability lengkap. | `config/logging.py`; `config/settings.py`: `LOGGING`; `config/views.py`: `liveness`, `readiness`. |
| Deployment details | Docker Compose, Caddy, Cloudflare Tunnel, Tailscale, WSL2, scheduled task. | DO NOT IMPORT kecuali ringkas di lingkungan operasi | Untuk SRS akademik cukup tulis "aplikasi web berjalan pada server dengan backend API dan database"; detail production masuk runbook/handoff. | `docs/production-runbook.md`; `docker-compose.production.yml`; `deploy/*`; handoff. |
| Showcase/demo mode | Quick login demo ada dan bergantung allowlist. | Sebagian | Boleh disebut sebagai fasilitas demo, bukan kebutuhan akademik inti. | `accounts/views.py`: `ShowcaseLoginView`; `config/settings.py`: `SHOWCASE_MODE`; `frontend/src/pages/auth/LoginPage.tsx`. |

Catatan NFR:

1. Detail seperti domain `app.corelasi.my.id`, Cloudflare Tunnel, Tailscale, systemd, dan WSL2 berguna untuk runbook, tetapi jangan mengubah SRS menjadi dokumen deployment.
2. Security yang paling relevan untuk SRS adalah autentikasi, otorisasi, validasi, HTTPS, session handling, dan backup/recovery.
